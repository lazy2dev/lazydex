# LazyDex v0.0.6 — Non-Functional & Broken UI Fixes

> **Tagline**: Polishing non-functional controls, broken UI stubs, and accessibility gaps into fully working features.
> **Origin**: Comprehensive UI audit identifying 24 HIGH, MEDIUM, and LOW severity issues across Dex, FilterSheet, Settings, Statistics, AddEdit, Browser, and Navigation.
> **Prerequisite**: Built on top of v0.0.3 / v0.0.4 / v0.0.5 after purging dead components.

---

## 1. Summary of Issues to Resolve (24 Items)

| Severity | Issue # | Component / File | Problem Description |
|---|---|---|---|
| **HIGH** | #1 | `DexScreen.kt` | Search button click handler is a dead stub (`{ /* Search */ }`) |
| **HIGH** | #2 | `DexScreen.kt` | Grid/List toggle state (`isGridView`) exists but has no UI toggle button |
| **HIGH** | #3 | `FilterSheet.kt` | `RangeSlider` imported & parameters accepted, but rating range filter UI never renders |
| **HIGH** | #4 | `DexViewModel.kt` | `dateRangeStart`/`dateRangeEnd` filters exist but have no UI controls |
| **HIGH** | #5 | `DexViewModel.kt` | No search query state or title search filtering |
| **HIGH** | #6 | `AboutScreen.kt` | "What's coming (soon)" and "Help translate" links are empty stub lambdas |
| **HIGH** | #7 | `AppearanceScreen.kt` | Theme preset carousel is purely decorative (no click handlers) |
| **MEDIUM** | #8 | `StatisticsScreen.kt` | Movie & TV categories missing from breakdown |
| **MEDIUM** | #9 | `StatisticsScreen.kt` | `inProgressCount` never displayed |
| **MEDIUM** | #10 | `StatisticsViewModel.kt` | Null initial state causes flash of zeros |
| **MEDIUM** | #11 | `UnifiedAddEditScreen.kt` | HTML preview hardcodes `Color.WHITE` (illegible in light mode) |
| **MEDIUM** | #12 | `Theme.kt` | `dynamicColor=true` default with no user toggle control |
| **MEDIUM** | #13 | `AppearanceScreen.kt` | `coverTheming` switch exists but never applied |
| **LOW** | #14 | `AppearanceScreen.kt` | Uses deprecated `Divider` instead of `HorizontalDivider` |
| **LOW** | #15 | `UnifiedAddEditScreen.kt` | Uses legacy `DatePickerDialog` instead of Material3 `DatePicker` |
| **LOW** | #16 | `UnifiedAddEditScreen.kt` | `extractAverageColor` decodes full bitmap (no `inSampleSize`) |
| **LOW** | #17 | `UnifiedAddEditScreen.kt` | Date picker clickable overlay on disabled `TextField` (a11y issue) |
| **LOW** | #18 | `BrowserViewModel.kt` | Browser search has no debounce or pagination |
| **LOW** | #19 | `MainShellScreen.kt` | Hardcoded Unicode `▾` in bottom nav label |

---

## 2. Implementation Plan by Git Commits

### Commit 1: `fix(dex): implement search query filtering and wire top bar search`
- Add `searchQuery` StateFlow to `DexViewModel` and `DexUiState`
- Update filter flow to match `searchQuery` against title/alt titles/author
- Replace empty `{ /* Search */ }` onClick with search UI

### Commit 2: `fix(dex): add grid/list view toggle action to DexScreen top bar`
- Add top bar action button with grid/list icon
- Toggle `isGridView` state

### Commit 3: `fix(filter): render rating range slider and wire date range pickers`
- Implement `RangeSlider` in `FilterSheet.kt`
- Add date range picker UI and wire to ViewModel

### Commit 4: `fix(settings): implement AboutScreen actions and theme/dynamicColor`
- Implement placeholder actions in AboutScreen
- Make theme presets clickable
- Wire `dynamicColor` toggle through to `MainActivity` + `LazyDexTheme`
- Apply `coverTheming` preference
- Replace deprecated `Divider` with `HorizontalDivider`

### Commit 5: `fix(statistics): add missing categories, inProgressCount, loading state`
- Add Movie/TV category cells
- Display `inProgressCount` in overview
- Add loading indicator to prevent zero flash

### Commit 6: `fix(addedit): fix HTML preview color, DatePicker, bitmap decode, accessibility`
- Change hardcoded `Color.WHITE` to theme color
- Replace legacy `DatePickerDialog` with Material3 `DatePickerDialog`
- Optimize `extractAverageColor` with `inSampleSize`
- Fix clickable overlay a11y issue

### Commit 7: `fix(browser): add search debounce and pagination`
- Add `debounce(300ms)` to search input
- Add pagination support for infinite scrolling

### Commit 8: `fix(navigation): replace hardcoded Unicode in bottom nav label`
- Replace Unicode `▾` with Material icon or clean styled label

---

## 3. Verification & Acceptance Criteria

- All unit tests pass: `./gradlew test`
- Clean build: `./gradlew assembleDebug`
- Zero dead/unused code warnings in Android Lint: `./gradlew lint`
