# LazyDex v0.0.5 — Polish, Automation & Safety Nets

> **Tagline**: Turning a feature-rich app into a reliable daily driver.
> **Origin**: Items from the original MVP plan that were deferred, plus new testing/infra needs discovered during development.
> **Prerequisite**: Built on top of v0.0.3 (foundation) and v0.0.4 (AniList sync + Komikku UI).

---

## 1. Automation & Background Tasks

### Auto-Backup (WorkManager)
- [ ] Scheduled backup — configurable: Never / Daily / Weekly
- [ ] SAF folder picker (`ACTION_OPEN_DOCUMENT_TREE`) — user selects destination directory
- [ ] Persisted URI via `takePersistableUriPermission`
- [ ] WorkManager `PeriodicWorkRequest` (min interval: Daily = 24h, Weekly = 168h)
- [ ] Timestamped filenames: `lazydex_backup_YYYY-MM-DD_HHmmss.lazydex`
- [ ] Safe copy flow: writes to temp file first, packages ZIP, copies to SAF only after validation
- [ ] Retention policy: keep last 30 backup files, auto-delete older
- [ ] UI feedback: last backup timestamp in Settings
- [ ] Error handling: failed backup → notification

---

## 2. External API Search UI

### AniList / MyAnimeList Search (Metadata Fetch, No Login)
- [ ] Search bar + result list in UnifiedAddEditScreen
- [ ] AniList GraphQL search query wired to search UI
- [ ] MyAnimeList REST search fallback
- [ ] Source URL auto-fill from selected search result
- [ ] Title, cover image, alt titles populated from selected result
- [ ] Category pre-set from search context

---

## 3. Testing Infrastructure

### Unit Tests (JUnit 5 Jupiter + MockK + Turbine)

**ViewModels:**
- [ ] `DexViewModel` — filter chain, genre/tag filter, category switch
- [ ] `UnifiedAddEditViewModel` — add flow, edit flow, scrape flow, validation
- [ ] `SettingsViewModel` — theme toggle, export/import state
- [ ] `BrowserViewModel` — source search, results
- [ ] `StatisticsViewModel` — aggregate observation

**Repository:**
- [ ] `add()` — UUID generation, `normalize()` called, lastUpdated set
- [ ] `update()` — `normalize()` clamping
- [ ] `incrementProgress()` / `decrementProgress()` — auto-complete, auto-date
- [ ] `replaceAll()` — atomic, normalize each item, exception mapping
- [ ] `setStatus()` — auto-date logic for all paths

**Scraper:**
- [ ] Valid/invalid URL, timeout, non-HTML response, large response
- [ ] Auto-charset sniffing, SSRF SafeDns, source matching

**Backup:**
- [ ] Round-trip: serialize → deserialize → serialize → compare
- [ ] ZIP export/import, schema version handling
- [ ] Edge cases: empty items, missing fields, bad values
- [ ] Merge: timestamps, local wins tie, URL dedup
- [ ] v1→v2 field preservation

**Domain:**
- [ ] Genre/tag normalization, status adaptive labels, auto-date logic
- [ ] `UrlNormalizer` — lowercase, strip fragment, trailing slash

### Instrumented UI Tests
- [ ] `DexScreen`: empty state, items, filter, sort, grid/list toggle
- [ ] `UnifiedAddEditScreen`: add/edit mode, validation, save, delete
- [ ] `SettingsScreen`: theme toggle, export/import
- [ ] `StatisticsScreen`: aggregate cards render
- [ ] Navigation: tab switching, screen transitions

---

## 4. Safety & Error Handling

### DB Corruption Recovery
- [ ] Detect `SQLiteDatabaseCorruptException`
- [ ] Error screen with [Reset Database] button
- [ ] Export current data before reset option

### Similar Title Detection
- [ ] Fuzzy-match on save: Levenshtein distance < 3
- [ ] Warning inline below Save button (non-blocking)

### Enhanced Validation
- [ ] URL format validation
- [ ] Max title length: 200 chars
- [ ] Max notes length: 5000 chars
- [ ] Max alt titles count: 20

---

## 5. Polish & UX Refinements

### Rating Color Interpolation
- [ ] Star color maps to rating value
- [ ] Smooth interpolation between breakpoints

### Animations & Transitions
- [ ] Shared element transition: card cover → detail cover
- [ ] Fade transitions between screens
- [ ] LazyColumn `animateItemPlacement()`
- [ ] Bottom sheet spring animation
- [ ] Progress change animation

### Accessibility
- [ ] Focus order follows visual order
- [ ] Star Rating: announce current rating
- [ ] Minimum touch target size (48dp)
- [ ] High contrast mode support

### i18n
- [ ] Extract strings to `strings.xml`
- [ ] Locale support: Japanese, Spanish, Simplified Chinese
- [ ] Locale-aware date/number formatting

---

## 6. Build & Release Tooling

### ProGuard / R8
- [ ] Custom `proguard-rules.pro` for serialization, Room, Coil, Koin, OkHttp, Jsoup
- [ ] Enable R8 full mode (`isMinifyEnabled = true`, `isShrinkResources = true`)
- [ ] Test release build

### Crash Reporting
- [ ] Sentry or Firebase Crashlytics integration (optional, non-blocking)

---

## 7. Infrastructure

### Backup Enhancements
- [ ] Encrypted backup (AES-GCM, passphrase)
- [ ] SHA-256 integrity check
- [ ] Partial import with diff preview

### Performance
- [ ] LazyColumn `key` parameter for stable identity
- [ ] Coil disk cache size limit
- [ ] Database index analysis
- [ ] Startup: defer non-critical Koin modules

---

## Summary

**v0.0.5 is the layer of robustness, automation, and confidence** that turns the feature-rich app into a reliable daily driver. Most items are non-visual — tests, safety nets, build tooling, and background automation.
